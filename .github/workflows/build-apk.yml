name: Build Android APK
on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Install Dependencies
        run: npm install

      - name: Auto Bump Version
        run: |
          node -e "
            const fs = require('fs');
            const data = JSON.parse(fs.readFileSync('app.json'));
            data.expo.android.versionCode = ${{ github.run_number }};
            data.expo.version = '1.1.${{ github.run_number }}';
            fs.writeFileSync('app.json', JSON.stringify(data, null, 2));
            console.log('v' + data.expo.version + ' (code: ' + data.expo.android.versionCode + ')');
          "

      - name: Prebuild
        run: npx expo prebuild --platform android --clean --no-install

      - name: Configure APK Signing
        run: |
          rm -f release.keystore
          keytool -genkey -v -keystore release.keystore \
            -alias unraid_alias -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass "UnraidApp2024!" -keypass "UnraidApp2024!" \
            -dname "CN=UnraidManager, OU=Dev, O=Personal, L=Unknown, S=Unknown, C=US"
          echo "KEY_ALIAS=unraid_alias" >> $GITHUB_ENV
          echo "KEY_STORE_PASS=UnraidApp2024!" >> $GITHUB_ENV
          echo "KEY_PASS=UnraidApp2024!" >> $GITHUB_ENV
          echo "KEY_BASE64=$(base64 -w 0 release.keystore)" >> $GITHUB_ENV

      - name: Build Release APK
        run: cd android && ./gradlew assembleRelease

      - name: List SDK build tools
        run: ls /usr/local/lib/android/sdk/build-tools/

      - name: Sign APK
        uses: r0adkll/sign-android-release@v1
        id: sign_app
        with:
          releaseDirectory: android/app/build/outputs/apk/release
          signingKeyBase64: ${{ env.KEY_BASE64 }}
          alias: ${{ env.KEY_ALIAS }}
          keyStorePassword: ${{ env.KEY_STORE_PASS }}
          keyPassword: ${{ env.KEY_PASS }}
        env:
          BUILD_TOOLS_VERSION: "34.0.0"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: signed-apk
          path: ${{ steps.sign_app.outputs.signedReleaseFile }}
          retention-days: 5

      - name: Upload to Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: ${{ steps.sign_app.outputs.signedReleaseFile }}
          tag: v1.1.${{ github.run_number }}
          name: "Unraid v1.1.${{ github.run_number }}"
          body: "APK Build - 修复播放按钮错位和闪退问题"
          token: ${{ secrets.GITHUB_TOKEN }}
          allowOverwrites: true
